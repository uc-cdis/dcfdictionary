$schema: "http://json-schema.org/draft-04/schema#"

id: "pb_pain_intensity"
title: Patient Baseline Pain Intensity
type: object
namespace: https://impowrgen3.wakehealth.edu/
category: clinical
program: '*'
project: '*'
description: >-
  Data describing patient baseline pain intensity characteristics
additionalProperties: false
submittable: true

systemProperties:
  - id
  - project_id
  - state
  - created_datetime
  - updated_datetime

links:
  - name: patients_baseline_pain_intensity
    backref: pb_pain_intensity
    label: derived_from
    target_type: pb_pain
    multiplicity: many_to_one
    required: true

required:
  - submitter_id
  - type
  - studies

uniqueKeys:
  - [id]
  - [project_id, submitter_id]

properties:

  $ref:
    - "_definitions.yaml#/ubiquitous_properties"

  patients_baseline_pain_intensity:
    $ref: "_definitions.yaml#/to_one"
