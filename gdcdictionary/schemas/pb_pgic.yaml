$schema: "http://json-schema.org/draft-04/schema#"

id: "pb_pgic"
title: Patient Baseline PGIC
type: object
namespace: https://impowrgen3.wakehealth.edu/
category: clinical
program: '*'
project: '*'
description: >-
  Data describing patient baseline PGIC characteristics
additionalProperties: false
submittable: true

systemProperties:
  - id
  - project_id
  - state
  - created_datetime
  - updated_datetime

links:
  - name: patients_baseline_PGIC
    backref: pb_pgic
    label: derived_from
    target_type: pb_pain
    multiplicity: many_to_one
    required: true

required:
  - submitter_id
  - type
  - studies

uniqueKeys:
  - [id]
  - [project_id, submitter_id]

properties:

  $ref: "_definitions.yaml#/ubiquitous_properties"

  patients_baseline_PGIC:
    $ref: "_definitions.yaml#/to_one"
